# S.A.V.I.A. V7.0 — Dependencias del Sistema
# Instalación: pip install -r requirements.txt
# NOTA: Para GPU NVIDIA instalar PyTorch con CUDA desde https://pytorch.org

# ── Core IA y Visión ──────────────────────────────────────
ultralytics>=8.3.0
opencv-python>=4.10.0
numpy>=1.26.0
Pillow>=10.3.0

# ── PyTorch (CPU base)
# Para GPU CUDA 12.1: pip install torch torchvision torchaudio --index-url https://download.pytorch.org/whl/cu121
torch
torchvision
torchaudio

# ── Inferencia SAHI (detección en alta resolución / drones 4K) ──
sahi>=0.11.19

# ── UI y Sistema Operativo Windows ────────────────────────
psutil>=5.9.0
pywin32>=306

# ── Reportes Tácticos PDF (offline) ──────────────────────
fpdf2>=2.7.9

# ── Mapas Geoespaciales ──────────────────────────────────
folium>=0.16.0

# ── Control de Torreta Arduino (opcional) ────────────────
# pip install pyserial
# pyserial>=3.5

# ── Telemetría MAVLink (opcional, solo Savia Activo) ─────
# pip install pymavlink mavsdk
# pymavlink
# mavsdk

# ── GPU AMD/Intel alternativa (sin CUDA) ─────────────────
# onnxruntime-directml